// # Array 를 기반으로한 Linked List 구현
// Array(초기화시 사이즈 할당) <-> ArrayList(사이즈 유동적할당)

/* Linked list 노드 생성 */
/*
class Linkedlist {
    constructor(data=null){
        this.head = (data&&Node(data));
    }
    //Getter - print
    
    get getlist() {
        let [data, next] = this.head;
        if(!node) {
            console.log("It's empty")
        }
        else{
            while(){
                console.log(next);
            }
        }
    }

    //Method - add
    add(data) {
        if (!this.head) 
            this.head = Node(data);
        let [curr, next] = this.head;
        while(next){
            
        }
    }
    
}

class Node{
    consructor(data, next = null){
        
    }
    const node = new Array(2);
    node[0] = data;
    node[1] = next;
    return node;
}

let head = Node(1);
let [hdata, hnext] = head;
let node = Node(5);
hnext = node[0];
console.log(head);



*/
